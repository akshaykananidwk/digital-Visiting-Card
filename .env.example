# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------
APP_NAME="Digital Visiting Card"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://example.com
# Extra hostnames this installation answers on, comma separated.
# APP_URL's host and its www/non-www counterpart are always trusted; list any
# further aliases here (an IP used for staging, a secondary domain) so links
# and assets are generated for the host the visitor is actually on. Hosts that
# are not listed fall back to APP_URL, which is what stops a spoofed Host
# header from ending up inside generated URLs.
APP_TRUSTED_HOSTS=
APP_KEY=
APP_TIMEZONE=Asia/Kolkata
APP_FORCE_HTTPS=true

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=digital_card
DB_USERNAME=root
DB_PASSWORD=
DB_PREFIX=

# ---------------------------------------------------------------------------
# Session
# ---------------------------------------------------------------------------
SESSION_NAME=dvc_session
SESSION_LIFETIME=7200
SESSION_SECURE=true
SESSION_SAMESITE=Lax

# ---------------------------------------------------------------------------
# Uploads (bytes)
# ---------------------------------------------------------------------------
UPLOAD_MAX_SIZE=5242880

# ---------------------------------------------------------------------------
# Razorpay (also configurable from Admin -> Settings)
# ---------------------------------------------------------------------------
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# ---------------------------------------------------------------------------
# SMTP (also configurable from Admin -> Settings)
# ---------------------------------------------------------------------------
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=
MAIL_FROM_NAME="Digital Visiting Card"

# ---------------------------------------------------------------------------
# GitHub auto-update (token is stored encrypted in the database instead when
# configured through Admin -> Updates)
# ---------------------------------------------------------------------------
GITHUB_REPO=
GITHUB_BRANCH=main
GITHUB_TOKEN=
